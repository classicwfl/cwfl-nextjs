import styles from './Logo.module.scss';

const Logo = () => {
    return (
        <a href="/" className={styles.logoWrapper}>
            <div></div>
            <div></div>
            <div></div>
        </a>
    );
};

export default Logo;
